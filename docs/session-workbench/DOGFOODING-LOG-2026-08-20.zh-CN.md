# Dogfooding 日志：用 codeg 开发 codeg（2026-08-20 起）

> P7 阶段的活文档。领导以人类身份通过 CDP 操作 dev 实例，用 codeg 自身的持久化
> 多 agent 协作开发 fork/rewind。**每条不便之处都记在这里**，按"现象 → 影响 →
> 改进方向"三段式，攒够一批评审后立项。

## 环境

- dev 实例：tauri dev + CDP 9222，主线 @ 今晚批次收官后（含 Room 搜索/过滤重构/
  IME 修复/@ 性能包）。复用了已在跑的 next dev（3000）。
- 首个课题：fork/rewind 第一切片（简报 `FORK-REWIND-TASK1-BRIEF.zh-CN.md`，
  在 wt/fork-rewind worktree）。
- 主编码会话：Claude Code + claude-opus-5[1m] + Xhigh + Bypass Permissions，
  工作目录 fork-rewind（分支 wt/fork-rewind）。
- 协作模型（用户指定）：Grok Build + Grok 4.6 —— 作评审位，等主编码者出活后拉群。

## 状态板（每次改动 O 条目状态时同步更新此表）

| # | 一句话 | 状态 |
|---|---|---|
| O1 | Ctrl+O 有应用内路径输入 | 好评·保持 |
| O2 | 两个"工作文件夹"控件同名不同性 | **已修·已激活** |
| O3 | 弹层触发对自动化点击不稳 | 排队（交互清扫） |
| O4 | worktree 分支识别延迟窗口 git_branch 疑问 | **已修**（grok 首单：三条写空路径改现场探测 HEAD；已并入主干） |
| O5 | 徽章/Room 搜索/Ctrl+K 批次验收 | 记录 |
| O6 | 门禁与 dev 共用 target 打架 | 已解决（target-gate 惯例） |
| O7 | 协调者体验观察 | 记录 |
| O8 | 5xx 打断回合无自动重试 | **用户已拍板·施工中**（A0+B-lite 做、A1/B-full 不做；grok run-mt2co6b4 在 wt/o8-retry） |
| O9 | Room @ 克隆幽灵会话 | **已修**（已激活） |
| O10 | dev/release 双实例可见性 | **已结案**（边界文档 DEV-RELEASE-INSTANCE-BOUNDARY 落库） |
| O11 | Room 消息卡死芯片 | **已修**（已激活） |
| O12 | 回复文件卡无内联 diff | **已修**（已激活） |
| O13 | human @ 房主被吞 | **已修·已激活**（人类 @ 协调者实测送达） |
| O14 | dev 重启记录 | 记录 |
| O15 | agent 建群侧边栏隐身 | **已修·已激活**（回填迁移已跑） |
| O16 | 右键漏浏览器原生菜单 | **已修**（已激活） |
| O17 | 工作台树不可拖拽 | **部分已修**（右键菜单全套；拖拽缺跨台 API 记缺口） |
| O18 | 群聊面板零 markdown | **已修**（已激活） |
| O19 | 会话中心群聊行不同构 | **已修·已激活**（领导 CDP 实测：全列渲染） |
| O20 | 文件面板拖动连带缩放 | **已修**（外层分隔条只动相邻面板；键盘路径/其余外层把手另记） |
| O21 | Room 时间线全量渲染卡顿 | **已修·已激活**（实测 54 行虚拟化）；@ 弹层渐进渲染另一半见 O38 |
| O22 | 启动器缺"新建群聊"入口 | **已修**（欢迎页页脚入口，复用共享对话框） |
| O23 | 路径绑定全程隐形 | **已修**（页头/对话框显示+目录选择器） |
| O24 | Room 工具调用渲染之争 | 裁决已定（不内联；活动桥排队） |
| O25 | grok 重启后恢复失忆 | **已修·已激活**（三工人带忆复活实测） |
| O26 | Room 面板无 Ctrl+F | **已修**（真高亮，CSS Highlight API） |
| O27 | Ctrl+K 文件搜索不自明 | **已修**（两 tab 自我说明文案） |
| O32 | 手动打开的 worktree 不挂主仓 Path | **已修**（待重启激活） |
| O33 | 工作台内 Room 行右键能力不及会话行 | **已修·已激活**（领导实测换台往返两遍，toast/分组/复原全对） |
| O34 | 会话跨台不唯一、无聚焦跳转、工作状态不可见 | **已修·三层全·已激活**（忙态像素级验证待真实在跑回合；Room 成员活动聚合另记） |
| O35 | 智能体设置"原生JSON/环境变量"不自明 | **已修**（面板自明文案已合并） |
| O36 | 分屏窗口临时最大化（herdr 式 focus） | **已修**（页签条 maximize/restore，当前分支） |
| O37 | Collection 行没有「新建会话」入口 | **已修**（collection-newsession 已合入） |
| O38 | @ 弹层每次全量重搜、无缓存 | **已修**（前缀复用 + 共享文件列表，O21 渐进渲染仍排队） |
| O39 | Claude 订阅 vs API 的 provider 切换 | 调研已交付；**设计 RFC 已备好等拍板**（`CLAUDE-PROFILE-RFC-2026-08-21`：走 CLAUDE_CONFIG_DIR 启动档，不再写用户原生文件） |
| O40 | 「不代为文件/终端」沙箱开关不自明 | **已修**（开关说明文案已合并） |
| O41 | 删 timer / 移群成员无确认弹窗 | **已修**（g5-quickwins 已合入） |
| O42 | Grok token 用量恒为 0 | **已修**（报真实 usage 而非上下文占用） |
| O43 | 会话卡「回复中」 | **已修·全结案**（turn 落错会话经审计五层防护无洞，仅记一处 background_watch 加固） |
| O44 | Codex 标题不同步进侧栏 | **已修**（title-sync：list 读入 DB + locked-title CAS） |
| O45 | 「待回复」独立面板 vs 会话中心过滤拥挤 | **已结案**（用户拍板：不做独立面板，会话中心已够用） |
| O46 | 任务看板：**前提修正**——已有四泳道看板 | **用户已拍板·第一期施工中**（run-mt2co7pp）；组织方式（分项目/群聊/Collection、vibe-kanban 参照）调研中（run-mt2co8i0） |
| O47 | API 额度剧耗告警 | 已定性（真实工作·经济性差）；**用户要求复查定性**（2026-08-21 白天，本批收尾后重核） |
| O48 | 群消息上下文经济：read_room 列表逐帖全文无截断 | **用户已拍板·施工中**（不做硬截断，改"逐步披露"接口：单帖 head+read_room_post 续读；run-mt2co71k） |
| O49 | D 盘耗尽事件（131G→601MB 曾致 dev 后端崩溃） | **已处置**（回收 91G+；制度见 DISK-WORKTREE-HYGIENE 文档） |
| O50 | Windows 桌面 cargo test 载入即死（0xc0000139） | **已修**（build.rs 给测试 exe 嵌 common-controls v6 manifest 依赖） |
| O28 | 群聊打开滚动条在顶部 | **已修·已激活**（实测打开即落底 distFromBottom=0） |
| O29 | 群聊缺时间线/日期分隔 | **已修·已激活**（实测本地化日期栏；Today/Yesterday 十语键记小尾巴） |
| O30 | 催办静音链定性 | 已定性（短路加固排队） |
| O31 | 待回复语义与进度显示 | **已修**（方向文案+每帖 M/N 已回徽标；MCP 投影缺口另记） |
| O51 | 新建群聊流程反直觉（先勾满 2 人才能建、首勾者暗成房主、房内拉人入口太小） | **方案已定·施工中**（改"先建群、进群拉人"：后端最少成员 2→1、发起人单选、房内拉人显眼化+空群引导；run-mt2csc5j） |
| O52 | 会话中心排序与时间列 | **已修**（用户委托拍板：排序+时间列一起换"最近活动"，钟不变量保住；领导亲改，313 测试绿） |
| O53 | 群聊聚合消息不带时间戳（用户："每个消息都带时间…自然有时间线"） | **已修**（聚合行左轨常显 24h 紧凑时间，悬停与回复按钮互换；非聚合行本就有时间；领导亲改，62 测试绿） |
| O54 | 分类树文件夹右键菜单无「新建群聊」 | 任务书已写·**等 O56 合并后派工**（要用统一 picker，避免又造一次性入口） |
| O55 | 配置 chip 只印值不印名（`Off`=Fast mode、`Default`=Agent 认不出） | **已修**（通用值词表 + `configChipLabel`，11 测试绿；领导亲改） |
| O56 | 房间成员管理跨组件重复（4 个入口各写一遍）+ 添加成员弹窗 button 套 button 报 hydration 错 | **施工中**（共享 SessionPicker + membership 服务 + 去重复大按钮 + 消歧行；run-mt2faacf） |
| O57 | 只能从群里加人，不能从会话侧加入群 | **施工中**（会话面板 ⋯ 菜单加「加入群聊」；run-mt2faayo） |
| O58 | 侧栏多选建群 | **本就已存在**（bulk action bar；且已按第一个选中会话的 collection 建群）——用户所问功能无需新建，需提升可发现性（排队） |
| O59 | Claude 接入方式无法按会话切换（订阅/API/中转） | **设计已定·后端施工中**（档=一套 settings，`CLAUDE_CONFIG_DIR` 注入，零 schema；run-mt2gl8qm） |
| O60 | 会话中心筛选行冒出一条竖滚动条 | **已修**（`overflow-x-auto` 令另一轴自动变 auto 的 CSS 陷阱；加 `overflow-y-hidden`，317 测试绿） |

## 观察记录

### O1. 好评：打开文件夹有应用内路径输入（Ctrl+O）

现象：`Ctrl+O` 文件夹对话框自带"输入目录路径..."文本框 + 目录浏览，原生选择器
只是快捷方式（workspace-folder-dialog.tsx:93 注释明说）。
影响：无头/自动化/远程场景全可用；这是 server 模式兼容性带来的正外部性。
方向：保持。

### O2. 两个"工作文件夹"控件同名不同性

现象：会话头部的文件夹面包屑和 composer 下方的文件夹切换器，title 都是
"工作文件夹: X"。头部那个是静态标签（代码注释："never a popover trigger"），
底部才是真选择器（aria-haspopup=dialog）。
影响：肉眼难分；自动化按 title 找控件必踩坑（本次踩了 3 轮）。对用户来说，
点头部面包屑无反馈也算轻微困惑。
方向：头部面包屑去掉按钮语义（改 span）或 title 改成"所在文件夹"；给底部
选择器加稳定 data-testid。

### O3. 弹层触发在自动化点击下时灵时不灵

现象：composer 下方文件夹选择器，同一坐标的 CDP 真实鼠标点击第一次无反应
（aria-expanded 保持 false），键盘 focus+Enter 也无反应，改成"先点面板任意处
激活 → 再点触发器（带 100ms 间隔的 move/press/release）"才打开。
影响：普通用户大概率无感（真实鼠标有自然间隔），但说明触发器对事件序列敏感，
可能与面板激活状态有关；自动化测试会 flaky。
方向：排查 pane 激活逻辑是否吞第一次点击（focus-within 切换时）；给 E2E 留
"点击两次"惯例或修根因。

### O4. worktree 分支识别有延迟窗口

现象：切到 fork-rewind（git worktree，.git 是文件）后分支 chip 短暂显示
"无分支"，数秒后变 wt/fork-rewind。
影响：轻微；但如果用户在窗口期发消息，会话记录的 git_branch 会不会写空？待查。
方向：确认 branch 探测的异步时序，或首次探测完成前禁用发送位的 branch 记录。
结案（2026-08-21，grok 桥首单）：**比疑问更糟——两条路径永远写空**，不只
是窗口期：① manager.rs Branch B（发消息时后端建行）直接传 None；
② chat_channel `/task` 抄 `folder.git_branch` 列——该列从不落库、恒 NULL。
③ chip 闪"无分支"是 open 路径用 null 覆盖轮询已解析的值。修复：三条写入
路径统一走 `detect_git_branch`（resolve_git_head，linked worktree 由 git 自理），
前端 open 路径 null-guard；带 tempdir 真 git 仓库回归测试（linked worktree
`.git` 为文件的断言）。已并入主干（Merge wt/o4-branch-window），门禁四号批验证。

### O5. 会话中心徽章/Room 搜索/过滤重构/Ctrl+K 手递手：实测全过

（属于今晚批次验收而非摩擦，记录在案：徽章=私信+Room 之和、点徽章直达
needs_reply 分段、筛选弹层 7 facet+Escape 逐层退、列表混排 Room 带"群聊"徽标、
Ctrl+K 无 agent chips、"在会话中心搜索 {query}"手递手预填成功。）

## 待办/跟踪

- [ ] 主编码会话完成第一切片 → 验收 diff → 拉 Grok 评审群
- [x] O4 的 git_branch 写空疑问 → 证实且已修（见 O4 结案）
- [x] IME #518：`c594615a` 已合入（composition 期间保住 @ 面板）；真输入法手敲仍建议抽空验一次
- [x] 2026-08-21 审计：范围是**昨天+今天**（北京 8/20–8/21）用户亲手提问，
      不是再往 8/19 以前翻。那晚合进当前分支的见 O36–O44。仍开着的见文末。

### O6. 门禁与 dev 实例共用 target 目录会打架

现象：dev 实例运行时跑 `cargo clippy --all-targets --features test-utils`，
tauri-build 复制 sidecar（binaries/codeg-mcp-*.exe）时 PermissionDenied——
运行中进程锁着文件。
影响：dev 长驻 + 门禁并行的工作流（正是 dogfooding 形态）必撞。
方向：门禁惯例改用 `CARGO_TARGET_DIR=target-gate`（已实践）；或文档里写明。

### O7. 协调者体验（正面居多）

- Host Control 建会话/Collection/Room 一条龙顺畅；协调者还能自查自纠
  （标题错字、自己标题被截断）。
- 协调者主动做了"编队卫生"（协作者进独立 Collection）和"省回合"设计
  （建会话不带首 prompt，分派走 Room 帖）——工具面表达力够用。
- 待观察：Room 帖能否可靠唤醒 grok-4.6 协作者干活（@ 唤醒语义）、工人完工
  回报的闭环。

### O8. API 503 打断回合后没有自动重试

现象：协调者回合中途遇 "API Error: 503 No available accounts"（上游临时故障），
回合直接终止，底部红条提示"usually temporary — try again"，但系统不自动重试，
会话就地闲置；需要人工发一条"继续"才恢复。
影响：无人值守的多 agent 编队里，一次瞬时 503 就能让协调链停摆（本次停了约
10 分钟才被哨兵发现）。
方向：对 5xx/临时性错误加有限次自动重试（指数退避）；或至少把"上次回合因临时
错误中断"变成显式可恢复状态（一键续跑 + 计入待回复徽章）。
审计结案（2026-08-21，grok 只读审计，报告 `O8-TRANSIENT-RETRY-AUDIT.zh-CN.md`）：
根因不是"缺一套重试"，而是 **`session/prompt` 的 JSON-RPC 失败被
connection.rs:8076 的 `?` 升级成整条连接死亡**（Claude 自带重试/AIR/Codex
willRetry 三套通道都健在，唯独这条裸奔）——503 → 杀连接 → 行 Cancelled →
队列无泵可用，正是"闲置十分钟"的机理。方案分级已备好：**A0**（RPC 失败改
非终端回合失败+连接存活）+ **B-lite**（`turn_failed_transient` code + Retry
按钮，不动状态枚举）= 建议的一刀；**A1** 自动重试默认不做（agent 侧可能已
持久化 user 消息，静默重发=双份 prompt）；**B-full** 新 ConversationStatus
不做（跨状态机+migration）。零内容窗口判定用现成 `TurnOutputProbe`。
两处待拍板：① adapter 在 RPC 失败后是否仍可 prompt（建议按 agent 门控或
二次失败再拆）；② A1 是否接受双份消息风险。动回合状态机，等用户醒后过目再施工。

### O9.（严重）Room @ 投递克隆幽灵会话 + session.stop 管不到

协调者第五批摩擦实锤（Room event 7a30351f，证据链完整）：@ 空闲会话时投递路径
**创建新会话**而非唤醒目标——幽灵数量精确等于 mention 数、同毫秒诞生、首条 user
消息=Room 信封原文；幽灵误认身份、MCP 连接失败、单个烧 455k token（一次性，
非持续泄漏——协调者用 message_count 自纠了"持续烧钱"的误判）。@ 忙碌会话无此
现象（猜测走了"closed Session is started"分支）。session.stop 对其返回
"no active managed runtime"。事故样本 4 个会话按指令保留未删。
影响：多 agent 协作核心路径的正确性 + 真金白银的 token。
方向：已立项派修（w-phantom），根因锁定投递唤醒分支。

### O10. dev 与 release 双实例同库但事件不互通

现象：dev 实例（我）建的 Room/会话写进共享 SQLite，但用户 release 实例的界面
收不到事件推送，侧栏停留在旧快照，重启或切工作台才刷新（用户实测"没看见群聊"）。
影响：双实例并行（dogfooding 常态）时另一侧界面陈旧，用户以为操作没发生。
方向：跨进程失效通知（文件 watcher 或 DB 版本号轮询兜底）；或至少文档写明
双实例的可见性边界。

### O10 更正（实证后）

之前"同库不同事件"的判断**错了**：dev 用独立 `codeg-dev.db`，release 用
`codeg.db`（%APPDATA%/app.codeg/ 下两个文件并存）。真实机制：会话因有磁盘转录
文件被对方实例的扫描导入（形成"影子会话"，包括 O9 幽灵的尸体也被 release 扫走）；
Room/Collection/mail 等纯 DB 构造**不跨实例**。用户侧困惑（"看不见群聊"）由此而来。
方向：dev/release 数据边界写进文档；影子会话考虑标注来源实例。

### O11. Room 消息卡片是"死"的：裸 room id、不可点、没有去群聊的路

现象（用户实测提出 + 代码确认）：会话记录里的 Room 消息卡片只有一个紫色
"Room" 标签加**裸 `rm_` UUID**（session-mail-card.tsx:137-138 直接渲染
`roomId` 为 mono 文本），既看不出是哪个群聊，也点不动——想去群聊现场只能
自己开会话中心翻列表。侧边栏也没有 Rooms 入口（sidebar.tsx:252-259 注释
自认 "Until Rooms get their own entry, the badge is the honest total"），
群聊唯一入口埋在会话中心行内。
影响：协作消息的"上下文一跳"断裂——人看到群聊消息却到不了群聊，裸 UUID
对人无信息量；群聊可发现性差（用户找不到群聊在哪，O10 双实例问题更放大了
这个困惑）。
状态：**已落地**（merge f7418004）。实现取向按工人举证修正：不是"会话中心
详情"，而是与全站一致的 `useOpenRoom` 直开 Room 标签页（会话中心点 Room 行
干的也是这个）。卡片显示群聊标题（事件驱动 catalog，不轮询），tooltip 保留
完整 id；解析不到（已删/影子库）降级为截断 id 不可点。连带修了 5 个测试
套件的 import 图断裂（stub chip）。侧边栏 Rooms 分区（每群未读/待回复）
属更大改动，仍待用户拍板。

### O12. 回复末尾的文件改动卡片不能就地展开 diff

现象（用户实测提出）：助手回复末尾的"新增文件/改动文件"卡片只有
「在编辑器打开」「工作区标签页看 diff」「在文件夹显示」几个出口，没有
**就地临时展开** diff 的方式——想瞄一眼改了什么必须离开聊天上下文。
组件注释里明确写了 "no inline diff"（reply-artifacts.tsx:55），是当时的
刻意取舍；但数据层 `FileChangeStat.diff` 已经带着每个文件的 unified diff
文本（session-files.ts:13-19），内联渲染不缺数据。
影响：审阅代理改动的最高频动作（"它到底改了啥"）要跳出会话流，多 agent
场景下每轮验收都在付这个切换成本。
状态：**已落地**（cherry-pick 13711864 + 测试匹配器修正 f7f160f0 前一提交）。
复用了 `UnifiedDiffPreview` 的 `embedded` prop——全仓零调用方、注释点名就是
给这个宿主预留的。手风琴单开、懒渲染、24rem 限高、区收起再开恢复上次
展开的文件；`+N/-N` 统计区并入展开开关。原有按钮全保留。

### O13.（严重）human 在 Room 里 @ 房主永远送不到——借来的 source 撞自我提及守卫

现象（重启后人类实测，链路逐层核实）：人类在 Room 界面发帖 @ 某成员时，前端把
**房主的会话 id** 借来当 source（rooms-page.tsx:539
`sourceConversationId: detail.createdByConversationId`——因为 event 表的
source_conversation_id NOT NULL，human 没有自己的会话 id）。当被 @ 的正是房主
（agent 建的协调房必然如此），后端把它当"自我提及"跳过：event 正常入库、正文
带结构化 `codeg://session/<id>` 链接，但**投递行为零、无唤醒、无徽章**，界面
毫无报错——人类以为喊到了，实际喊了个寂寞。
影响：hub-and-spoke 编队里人类给协调者下指令的主通道静默失效；恰好是最常用
的形态（协调者建房）。O9 修好后这是 Room 唤醒链路上暴露的下一层。
状态：**已落地**（merge 16c898b8）。守卫改为"作者身份规则而不是 id 规则"：
只有 author_kind='session' 才跳过自我提及；human 帖 @ 房主正常投递+唤醒+
挂义务+徽章，human @all 也覆盖房主。工人还查出并连修了读侧三处同源误判
（都是把"借来的 id==作者"当真）：房主永远看不到 human 帖的频道未读、
human 的提问在房主卡片上 needs_reply/awaiting_reply 双算。长线的 human
专属 source 语义仍在待议。Rust 改动需下次后端重启生效。

### O14. dev 重启记录（2026-08-20 深夜）

停机窗口约 12 分钟：`cargo clean` 释放 211GB（磁盘 98%→89%）、全量重建、
带 CDP 重启。新二进制激活：O9 幽灵修复、MCP 分阶段超时、销账回报、claude
订阅 env 防线。重启后首次唤醒协调者：冷启动握手在新预算下一次成功
（连接指示"已连接"）。O13 正是重启后第一次实测 Room @ 链路时抓到的。

### O15. agent 建的群聊在侧边栏没有家（用户两次实测催办）

现象：MCP/Host Control 建的 Room 在分类树里彻底隐身——树按 collection_id
或 root_folder_id 归桶，agent 建群两者全 NULL（三个入口共用的
resolve_placement 只有"成员同 collection"一条隐含推导，agent 编队成员
必然分散）；用户两次截图问"群聊到底在哪"。
影响：群聊只存在于数据库里，用户从树上永远点不到；叠加 O10 双实例困惑。
状态：**已落地**（merge d74d2c9a）。三层：① service 层 resolve_placement
第 4 级兜底——建群打戳创建者会话的 Path（worktree 折到仓库根、chat 草稿目录
不猜）；② migration 回填存量 NULL 行（同一条推导，幂等，down 有意 no-op）；
③ 前端树根"未分类"兜底组，救 NULL/NULL 和"Path 被关掉"两种隐身，写死不变量
"an active Room must always be reachable from the sidebar tree"。前端已
经 HMR 实测生效（CDP 探到兜底组含协调房）；①② 需下次后端重启生效，届时
协调房会自动归到 codeg Path 下。已知遗留：Collection 自身 root 不在
pathRoots 时整个 Collection 连带内容物隐身——是 Collection 可见性问题，
单独立项。

### O16. 右键漏出 WebView2 浏览器原生菜单（用户实测截图）

现象：在没有组件级自定义右键菜单的地方右键（例：侧边栏"工作台"区的会话行），
弹出的是浏览器原生菜单（返回/刷新/另存为/打印/更多工具/检查）。全仓没有任何
全局 contextmenu 拦截（grep 只有 pet 页一处局部用法）。
影响：桌面应用里露浏览器菜单观感突兀；"返回"可能把 SPA 整个导航走；
"另存为/打印"全是无意义出口。
方向：已派 w-contextmenu——document 级全局拦截 + 三个例外（可编辑元素保留
原生复制粘贴、data-native-context-menu 逃生舱、dev 模式 Shift 右键保住
"检查"）；不碰既有自定义菜单。工作台行的应用级右键菜单另立项（见 O17）。

### O17. 工作台树不可拖拽，affordance 与分类树不一致

现象：侧边栏"工作台"区的树是纯导航（workbench-tree.tsx:124 注释自认
"navigation, not a mirror"，全文件无任何拖拽代码），但它和下面处处可拖的
分类树长得同构；用户试图拖动时无任何反馈，点击即切换工作台，"特别怪"。
影响：相邻两棵树一个能拖一个不能，用户无法预判手势结果；"把会话挪到另一个
工作台"这个自然操作没有任何入口（拖不动、右键还漏原生菜单=O16）。
方向：设计已定——工作台树行支持跨工作台拖拽移动 + 应用级右键菜单（关闭
标签页/移动到工作台 N/在新工作台打开），排在在飞项落地后立项。

### O18. 群聊面板零 markdown 渲染；diff 展示裁决为"不做卡片"

现象（用户实测）：Room 帖正文经 roomMessageBodyParts（纯文本切分+@ 芯片）
平铺，agent 发的围栏代码/列表/粗体全按纯文本显示；会话侧的 Streamdown
渲染栈（message.tsx:343 起，含 link-safety/rehype-allow-codeg 安全插件）
Room 完全没接。
影响：编队沟通的主频道可读性差，agent 天然说 markdown。
裁决：markdown 要（复用会话渲染栈+同套安全插件，@ 芯片语义不得回退，
紧凑样式）；reply-artifacts 式 diff 卡**不要**——Room 是协调频道，重资产
留在产出它的会话里，```diff 围栏按代码块渲染，要看全量点芯片跳源会话。
状态：**已落地**（merge 01744e96 + 测试匹配器修正 becc9518）。工人举证
了关键事实：既有管线里 codeg:// 徽章是**惰性的**（span 无点击），@all/
@human 根本不是 reference——所以走"先掩码提及、再渲染、a 覆盖三个 Room
目的地"的方案；围栏内 @ 不动（代码就是代码）；软换行开启（纯文本帖两行
不粘连）。CDP 实测 HMR 生效：36 帖全走 markdown、671 个行内代码、27 个
芯片保留。领导修正 2 个新测试的选择器（Streamdown 的加粗是
span[data-streamdown=strong] 不是 strong 标签）。

### O19. 会话中心里群聊行与会话行不同构（用户截图双箭头）

现象：会话中心列表里 Room 行没有多选 checkbox、没有工作台/分支等元数据列，
与上下的会话行错位；用户两个箭头分别指着缺 checkbox 和空荡的中列。
影响：视觉错位；Room 进不了多选批量操作面。
方向：并入"交互契约清扫"（见 O16/O17 的系统性动作）——对象×手势矩阵里
Room 行的空格子要么补齐（checkbox+批量操作语义）要么明确写"无+为什么"。

### O20. 多面板拖动不对称：调右边左边跟着动，调左边右边不动

现象（用户实测）：三面板布局里拖右侧分割线，左侧面板宽度跟着变；拖左侧
分割线，右侧不动。
根因（代码定性）：面板是嵌套二叉分割树（group-split-handle.tsx +
tab-group-layout），拖把手改的是**所属 split 节点**的边界比例。右侧把手
是外层节点边界——左+中作为一个子树整体按比例缩放，左面板被连带；左侧
把手是内层节点边界——只在左、中之间分配，右面板无关。数学上自洽，
但用户感知是"不可预测"。
**用户复现修正（重要）**：全是会话面板时无此问题；最右侧是**文件面板**时
必现（全文件面板也必现）。说明文件面板进分割树的拓扑与会话面板不同
（嵌套方式导致其把手落在外层节点边界），不是所有把手都错。调查范围
收窄到"文件面板如何插入分割树"。
裁决：改成 VS Code SplitView 语义——**拖把手只重分配紧邻的两个面板，
其余面板绝对宽度不变**（把外层比例变化换算成对相邻兄弟的补偿），顺带
统一文件面板与会话面板的插入拓扑。涉及分割树数学，单独立项，排在
在飞项后。

### O21. 群聊 @ 弹层卡"搜索中"（未复现，待现场取证）

现象（用户截图）：Room composer 输入 @ 后弹层三个 tab（会话/文件/提交）
只显示"搜索中..."，无任何结果。
静态核查：设计上有双保险——成员组纯本地计算、文件/提交搜索有 3s 超时
竞速兜底（room-mention-search.ts:137,165），永久卡死与代码矛盾；领导
CDP 探测时现场已消失（弹层关闭）。两个假说：① 用户截图落在 3s 窗口内
（后端文件/git 搜索慢或挂起时，弹层最多空转 3 秒才降级到只剩成员）；
② 防抖 effect 被 search 身份高频变化重置（members 数组每次 detail
刷新都是新引用）。
方向：无论哪个假说成立，正确修法一致——弹层支持**分组渐进渲染**：
成员组（本地零延迟）立即出现，文件/提交组异步补齐，各 tab 独立 loading。
涉及 suggestion-popup 共享契约，等 w-roommd 落地后立项；重启窗口做一次
现场复现取证。

### O22. 新建会话空态页应有"新建群聊"入口（用户提议，采纳）

现象：新建会话页是"今天准备做些什么"的启动器（模板卡+agent 选择+
composer），但只能起单会话；建群入口仅在侧边栏导航。
裁决：采纳——启动器页面加一个轻量"新建群聊"次级入口（不喧宾夺主，
主流程仍是起会话）。小任务，与 Room 可发现性批次（侧边栏 Rooms 分区
待拍板项）一起排。

### O23. 群聊路径绑定链路全通但全程隐形（用户以为没绑定）

现象（用户实测 + DB 核实）：用户把协调房挪进 collection 后，后端
`assign_to_collection` 其实**正确联动了路径绑定**（DB 实测：
collection_id=5、root_folder_id=4=fork-rewind worktree 目录，@ 的
文件/提交搜索从此有了作用域）——但界面零反馈：Room 页不显示绑定
目录，"管理附加路径"对话框只说"在其绑定文件夹之外"却不显示绑定的
是哪个文件夹（未绑定时也不提示），挪动后已打开的 Room 标签页详情
可能不刷新。用户由此判断"没有自动绑定、@ 搜索无法使用"。
影响：功能对但不可见=等于没做；用户对 @ 搜索失灵的归因也被带偏
（连到 O21）。
裁决：自动绑定**要做且已做**（建群打戳+回填 migration+挪动联动，
前两者待后端重启激活）；要补的是可见性三件套——① Room 页/附加路径
对话框显示当前绑定目录（未绑定时明示）；② 挪动后详情即时刷新，@
作用域立即生效；③ 附加路径对话框的手填绝对路径改为复用现成的目录
选择对话框（O1 记过：workspace-folder-dialog 自带路径输入+目录浏览，
直接复用）。与 O21 渐进渲染同批立项。

### O24. 竞品对照：Room 里要不要渲染工具调用（裁决：不内联，做活动桥）

背景（用户问 openagents 机制 + 是否该学它渲染工具调用）。取证结论：
- **OpenAgents**：workspace="agent 版 Slack"，agent 是**常连客户端**
  （agn launcher 守护进程 + workspace token + WebSocket），线程就是
  agent 的唯一界面——适配器把 CLI 的结构化事件流（stream-json 等）
  逐条翻成聊天消息，所以工具调用天然出现在聊天里。@ 两人配合没有
  显式时序机制：都收到消息，评审者盯着频道等干活的发完工帖（涌现式）。
- **Multica**：issue 制。工具调用渲染在 **issue 的执行日志时间线**
  （可回放、带时间戳、计 token），聊天/评论流保持干净。
- **Buzz**：agent 是频道成员、@ 才动。工具调用在 **"View activity"
  审计面板**（每步可查、双签名审计），频道消息流干净。
裁决：codeg 与 Multica/Buzz 同构——每个 agent 有自己的完整会话页，
工具调用在那里已全保真渲染，Room 是协调层。**不把工具调用流内联进
Room**（会把信号密度打没，且与 O18"重资产留在会话里"一致）；补的是
**活动可见性桥**：Room 成员卡显示实时状态（空闲/生成中+当前回合摘要），
点击直达会话——把 openagents"看得见 agent 在干活"的好处拿到，不付
刷屏代价。openagents 真正值得学的差异点另记：常连模式让 agent 可以
"自己捡活"（我们是 @ 唤醒制，更省但无环境感知）、共享浏览器/共享
文件面板（我们用共享仓库/worktree 覆盖大部分场景）。

### O25.（严重）Grok 会话重启后恢复必失败：cwd 形态不一致查不到桶

现象（用户截图红条 + 日志/磁盘双证）：dev 重启后首次唤醒 grok 工人，
session/load 报 `FS_NOT_FOUND "系统找不到指定的路径"`，codeg 按设计
回退 session/new（红条"starting new"）——**agent 侧上下文失忆**，codeg
转录靠 link 机制照常渲染。日志两条命中（00:08/00:10，均 Grok、均
fork-rewind worktree）。
根因（烟枪）：grok 的会话文件**还在**——
`~/.grok/sessions/%5C%5C%3F%5CD%3A...`，目录键是 `\\?\D:\...`
规范化长路径形态；恢复请求传的是 `D:/code/...` 普通形态，字面查桶
落空。grok 按规范化 cwd 建桶、按请求参数字面查桶，两头形态不一致。
影响：**每次 codeg 重启后所有 grok 会话首次恢复必失忆**——编队工人全是
grok 时，重启等于全员洗脑；第三轮哨兵冻结与此强相关。
方向：已派 w-grokresume——仅对 Grok 把 load/new 请求 cwd 规范化为
canonicalize 形态（失败回退原值），其他 agent 字节不变；落地随重启
窗口激活。连带待办：重启前先让在跑回合收口的"温重启"程序化。

### O26. Room 面板没有 Ctrl+F（能力抽象缺口的典型样本）

现象（用户实测）：群聊时间线无法页内查找。
定性：**查找条早就存在**——conversation-find-bar.tsx +
conversation-find-highlight.ts，Ctrl+F 监听挂在 message-list-view.tsx
（会话转录面）；Room 面板独立渲染，没有复用。这不是"没做过"，是
"新面没继承已有能力"——UI 行为层缺公共底座的典型样本，支撑"抽公共件、
不推倒重构"的架构裁决。
方向：已派 w-roomux（四件套：Ctrl+F 复用、绑定路径显示、附加路径改
目录选择器、挪动分类后详情刷新）。

### O27. Ctrl+K 文件搜索用途不自明（+"会话该不该在这"是旧决策，非漏改）

现象（用户实测）：搜索面板文件 tab 不知道搜的是什么、哪个范围、干什么用。
定性：① 文件 tab = **按文件名/路径模糊匹配当前文件夹**（不搜内容），点击
直接打开文件——VSCode Ctrl+P 的定位（SEARCH-SYSTEM-SURVEY 已记）；有用
但完全不自我说明（占位符只写"搜索文件"，不说"按文件名跳转·当前文件夹"）。
② 用户记忆"会话不该出现在搜索面板"与当时裁决不符：调研后定的是**保留但
轻量化**（去 agent chip、去正文分支、深搜手递手交会话中心）——已落地，
截图所见即轻量版。Ctrl+K 心智 = "直接跳到任何东西"，撤会话反而破直觉。
方向：文件 tab 占位符/空态写明用途与作用域（并入交互契约清扫的
"自我说明"轴：每个搜索入口必须标注搜什么·哪个范围·点了去哪）。

### O28. 群聊每次打开滚动条停在最上方

现象（用户实测）：打开 Room 标签页视口停在最旧的帖子，要手动滚到底才见最新。
会话面板打开即定位最新——同类面不同性。
方向：Room 家族小修（打开/新帖锚定最新，用户上滚阅读历史时不抢滚动，参照
会话面板既有锚定语义）。

### O29. 群聊缺时间线/日期分隔

现象（用户实测）：Room 时间线连续平铺，无日期分隔/时间锚点，长群聊无法按
时间定位；会话面板有成熟的时间组织。
方向：并入转录面骨架迁移——骨架统一后日期分隔/时间锚/跳转都是继承来的。

### O30. 催办静音链定性：机制活着，被 grok 失忆卡死过

现象（用户 in-codeg 代理静态分析 + 领导运行时取证合并）：催办机制存在且在跑
（5 分钟未读阈值、5 秒扫描、每欠账周期最多催 3 次）。静音机理：催办入队唤醒
→ 唤醒因 grok 恢复失忆失败 → 条目卡队列 → has_pending_mailbox_attention
短路静音后续催办（collaboration_reminder_runtime.rs:191）。重启+grok 修复后
实测链条自解：00:42 扫描给三个工人各排一条催办，三工人全部 in_progress
带忆干活。
遗留：① 短路加固（卡住的 attention 应有超时重投/上报，非永久静音）；
② collaboration_reminder.rs:42 注释与 SQL 实义不符（用户代理发现）；
③ 队列里 3 条 reminder 条目的消费待观察。
裁决：催办自动=已有且该有；自动替 agent 回复=不做（回复是语义行为）。

### O31. 待回复语义澄清与进度显示缺口（用户连问，已核实源码）

语义现状：**带 @ 才挂账**。human 帖 expectsReply = mentionAll || 有目标
（rooms-page.tsx:576）；无 @ 的普通帖零义务。@ N 人 = N 份独立义务（每人
一份 delivery+obligation），作者等待数=未答复人数，某人回帖销自己那份账
（销账回报已做）。agent 帖由 MCP 调用自行声明是否要回复。
无限唤醒之忧：不存在结构性无限环——回帖销账不必然再 @ 出新账（agent 可选
不带 ask 的回帖）、催办每周期上限 3 次、agent 自我提及不投递。理论上双方
互相 ask 可以人为打乒乓，但每一拍都要 agent 主动选择 ask，属行为而非机制。
缺口（采纳排队）：① 卡片加 N/M 已回进度；② 待回复/等待回复方向性 tooltip。

### O32. 手动打开的 git worktree 不挂主仓 Path（collection 分类问题的根）

现象（用户提问引出 + DB 实证）：Ctrl+O 直接打开一个 git worktree 目录时
parent_id 留 NULL（只有 codeg 自建 worktree 走权威写入），于是它在侧边栏
与主仓平级成独立 Path——fork-rewind 现行案例。这是"以路径分类是否合理"
之问的真正病根：模型形状早就对（parent_id + COALESCE 归位），缺的是
打开时的 git 关系检测。
状态：**已修**（merge 7670aa28，待重启激活）。打开目录时读 .git 文件解析
gitdir 指向，主仓已注册则自动挂靠（已有 parent 不覆盖）；启动期回填任务
补存量（migration 惯例不碰文件系统，有先例举证）；宽容路径比较复用
parsers 的 normalize（库内正反斜杠混存实证）。工人主动上报连带影响：
work_task 的"必须在项目根跑"检查现在能正确拒绝 worktree（以前是不知情
放行），语义更对但属用户可见变化。UI 提示/解绑另批。

### O33. 工作台树里 Room 行与会话行右键能力不对等

现象（用户实测）：O17 落地后会话行有完整菜单（关闭/移动到工作台/新台打开），
Room 行只有"关闭标签页"。工人当时已记缺口：Room 的 workbench 在建群时固定，
后端无重指派 API；且非激活工作台的 Room 标签树上根本不渲染。
方向：归交互原语批——先补后端 Room 换台能力，再菜单对齐；用户明确说"不是
要完全一致，而是相通的功能要都有"，对齐范围以能力矩阵为准。

### O34. 会话↔工作台归属语义与"谁在工作"可见性

现象（用户四连问，2026-08-21）：工作台里会话要不要唯一；工作台要不要像
paseo 那样显示工作状态（哪些会话在工作/最近工作过/哪个工作台里有活）；
查看某会话时进已有工作区还是新开窗口；若一个会话能出现在多个工作台，
进入时是否要选台。
现状（代码实证）：workbench-session-tabs.ts:25-44 的 appendConversationTabs
只在**本台**去重（conversationIdsInTabs），跨台可重复打开同一会话；无
"聚焦已开页签"的跨台跳转；工作台页签无运行状态指示（Room 成员卡已显示
running，说明后端状态源现成）。
裁决（领导）：
1. **跨台唯一，一个会话一个家**。会话是有状态的活流不是文档，双开只带来
   "我在哪个副本里打字"的混乱和双份渲染；监控聚合的需求交给侧边栏/会话
   中心/状态汇总，不靠复制转录面。唯一化后"进哪个台"的选台问题自动消解。
2. **打开即聚焦**：任何入口（侧边栏/会话中心/群聊提及跳转）打开已开会话
   = 跨台跳转聚焦既有页签；未开 = 当前台新开页签；新窗口只走显式动作。
3. **工作状态三层可见**：页签圆点（运行中/需注意）→ 工作台级汇总徽标
   （N 个在忙）→ 会话中心按最近活动排序兜底"最近工作过"。状态源复用
   Room 成员卡 running 的同一后端事实，不另起炉灶。
状态：裁决已定·排队（交互原语批：动 tab-store 唯一化+跳转语义+页签
chrome，与 O33 的 Room 换台 API 同批设计）。

### O35. 智能体设置面板不自明：原生 JSON / 环境变量是啥、写到哪，没人看得懂

现象（用户提问 2026-08-21）："智能体设置有什么原生json和环境变量，这些
不知道是啥，codeg是自己维护settings吗，不是根据用户claude code的设置来吗"
——产品所有者本人看不懂这两个字段的语义和作用域。
现状（代码实证）：
- "原生 JSON 配置"= 用户真实的原生配置文件本体：Claude → ~/.claude/
  settings.json，Gemini → ~/.gemini/settings.json，OpenCode/Codex/Cline/
  KimiCode 各有映射（acp.rs:7580-7592 agent_local_config_path）。读取后
  合并式写回，快捷字段（API URL/Key/Model）是同一文件的联动视图；前端
  还刻意串行 env→config 防两个写者交错写 settings.json
  （acp-agent-settings.tsx:11564 注释）。**不是 codeg 私有副本**。
- "环境变量"= codeg 数据库里的每智能体 env 覆盖层，只注入 codeg 拉起的
  该智能体进程，merge_agent_env 给最高优先级（acp.rs:7507）；存在意义：
  无头认证（如 XAI_API_KEY）、让 codeg 会话与终端会话可用不同配置而不
  污染全局文件。
影响：语义没错但完全没自我说明——用户以为 codeg 另搞了一套 settings，
信任成本高；也看不出"env 覆盖层优先级最高"这种会改变行为的事实。
与 O27（搜索不自明）同族：功能对、话没说。
方向：每个区块标注"它编辑的是哪个文件"（真实路径+可点开）与一句话作用域
（"仅作用于从 codeg 启动的会话，优先级高于文件配置"）；原生 JSON 区块
标题直接带路径。归交互清扫/自明性批。

---

## 审计：最近 100 条用户 prompt vs 本日志（2026-08-21）

来源：Claude Code 会话 `004e7a7d-7007-4bee-8c8f-9ca6c5e93c73`
（标题「Codeg 重构与 UI 优化规划」，cwd 主仓 codeg；共 176 条真人提问，
取 2026-08-19 14:36 → 08-20 18:22 最近 100 条；9 条带图）。过滤了
teammate idle、task-notification、slash compact 的机器注入。

协调者曾声称「你提的每一条都在 01–035 状态板上，上次还做过 30 条 prompt
的零遗漏审计」。对照这 100 条：**O1–O35 没有覆盖完**。漏的不是进度指令
（编译多久、思考强度、compact），而是用户点名的产品摩擦。已补 O36–O45。

仍算已覆盖、不另立条的：O15 群聊侧栏隐身（065/066/073/074）、O20 分屏
拖动（077）、O21 @ 卡搜索中（077）、O24 工具调用是否进 Room（078）、
O26 Ctrl+F（080）、O27 Ctrl+K 文件搜索/会话 tab（049/080）、O31 待回复
语义（086/087）、O32 路径分类/worktree（083）、O33 工作台 Room/会话右键
（087/099）、O35 原生 JSON/环境变量（097）。O5 徽章=私信+Room、七 facet
过滤，对得上 006 里 G5-11/过滤，但对不上后来「过滤拥挤、待回复不该独立
面板」（→O45）。

不算漏：rebase/门禁/handoff/停 subagent/用 GitHub 还是本地文档/项目改名。

### O36. 分屏窗口临时最大化（herdr 式 focus）

现象（2026-08-19 16:37）：工作台多分屏时，想让某一窗临时占满再一键恢复。
用户点名 herdr 有这种细节。
影响：多会话验收时小窗看不清；现有 O20 只处理分割线数学，不提供 focus。
状态：**已修**（当前分支）。`4caff43f` 状态、`eb393d44` 全屏+Esc 退出、
`921b1614` 页签条按钮（2026-08-20 02:16）。O20 分割线数学仍排队。

### O37. Collection 行没有「新建会话」入口

现象（2026-08-19 20:27）：侧栏 collection 没有信件/新建会话 icon，用户
问「新建 session 从哪里新建？难道从未分类拖进来？」。随后派
`w-collection-newsession` 紧急热修；workbench 新建会归入当前台，
collection 新建却进未分类，机制不同。
影响：日用主路径找不到「在这个分类下开会话」。
状态：**已修**。`a7ed0a5e` Merge wt/collection-newsession（2026-08-20 04:42），
`7bacf20e` closeout。Workbench 新建归当前台、Collection 新建归该分类，
机制本来就不同。

### O38. @ 弹层每次全量重搜、无缓存

现象（2026-08-19 22:54）：每次 `@` 都重新触发搜索；用户问 Claude Code /
Codex 是否也这样，建议缓存上次结果同时后台刷新。点名会话中心、待回复
面板同样。
影响：卡顿、重复扫盘；O21 是「卡搜索中」的超时/渐进渲染，不是缓存。
状态：**已修主路径**。`6ad15862` perf(@ panel): stop blanking results, reuse
prefix matches, share the file listing（2026-08-20 07:57）。O21 渐进渲染
（成员立即出现、文件/提交异步）仍排队。

### O39. Claude 订阅 vs API 的 provider 切换

现象（2026-08-20 10:44，带 Monet/Paseo 截图）：想同时用 Claude Code 订阅
和 API，问要不要像 Monet/Paseo 那样做 provider 切换。
影响：O35 只解释了「原生 JSON = 用户 settings.json、环境变量 = codeg
覆盖层」，没有回答「订阅通道和 API 通道怎么并存/切换」。
状态：设置里**早已有**三态（`b64976e4`，2026-04，official_subscription /
custom / model_provider）；订阅 env 泄漏 `542f1c5d` 已修。用户 8/20 仍
对照 Monet 觉得「没有切换」= 自明性失败，不是从零缺功能。会话级「这个
Session 用订阅、那个用 API」仍未做。归设置自明批，不要当成未开工。

### O40. 「不代为文件/终端」沙箱开关不自明

现象（2026-08-20 14:46/14:52）：用户看到「codeg 不再代为提供文件访问和
终端命令…请仅在 Agent 已配置沙箱时开启」，以为和 CLI 一样，问 Paseo/
Buzz 有没有、这是干嘛的。
影响：与 O35 同族（功能对、话没说），但是另一块设置，不是 JSON/env。
方向：自明性批——一句话写清「关 = Agent 自己的 FS/shell + 它的沙箱；
开 = 仍走 codeg 托管（无沙箱）」。

### O41. 删 timer / 移群成员无确认弹窗

现象（2026-08-19 16:23 第一梯队清单 G5-4）：timer 删除、群成员移除没有
确认，手滑就没了。
影响：不可逆操作无刹车。
状态：**已修**。`9c0452b3` Merge wt/g5-quickwins: confirm dialogs for timer
delete and member removal, clickable member count（2026-08-20 02:05）。
「N 位成员可点」一并落地。

### O42. Grok token 用量恒为 0

现象（同上清单第三梯队）：grok 的 token 用量显示恒为 0；用户说上游有修复。
影响：编队全 grok 时用量面板说谎。
状态：**已修**。`47ecc25e` / `6d851ed1` report real token usage instead of
context occupancy（2026-08-20 03:11）。

### O43. 会话卡「回复中」/ turn 落错会话

现象（同上第一梯队 1–2）：会话卡在「回复中」永远转圈（上游 1e3e5a10）；
turn 落错会话 / 新会话孤立历史（上游修过，我们状态机已重写，需确认
有没有同类洞）。用户问「这些也安排了吗」。
影响：稳定性硬伤；dogfooding 里一旦撞上就是编队停摆。
状态：「回复中」卡死 **已修** `2ed571e5`（2026-08-20 00:36，对照上游
1e3e5a10）。turn 落错会话 / 孤立历史：我们状态机已重写，**未单独核对该
洞是否还在**，保持待核实，不要当成还没排。
结案（2026-08-21，r-turnroute 只读审计）：**五层防护、无现存洞**——
① fork 后连接绑定切换前有 in-flight turn 排干等待；② manager.rs
1417-1423 后端身份核对（连接声称的会话 ≠ DB 绑定则拒写）；③ 事件写入以
DB 绑定为准而非连接自报；④ fork 交接期消息进队列不进旧会话；⑤ 有回归
测试钉住交接窗口。仅一处非缺陷加固建议：background_watch rearm-on-fork
会丢弃未消费的 out-of-turn 转录尾巴（只影响侧栏显示，不产生错归属），
已记入排队项。
尾巴加固裁决（2026-08-21，领导核查后**不做**）：本想派工"rearm 前把旧
会话尾巴最后读一次再发出去"，读了前端 overlay 退役机制后自行撤销——
overlay 条目按**字节水位**退役且不带会话身份
（conversation-runtime-store.ts:525-544 `retireCoveredBackgroundTurns`，
条目 watermark ≤ 详情 watermark 即退役）。fork 后新转录是旧文件的近全量
拷贝，水位数值≈旧文件尺寸：补发的旧尾巴要么下一次详情拉取就被立即退役
（白发），要么在 rewind 到早锚点时水位盖不住、**永远钉死在新会话视图里**
（比现状更糟）。要做对必须先给 overlay 退役加会话作用域——那是 fork/
rewind 显示语义的真改造，不是有界小修。现状（丢一个轮询窗口的侧栏尾巴、
无错归属）恰是此机制下的安全行为。结论：维持现状；若未来立项"overlay
会话作用域"，此项并入。

### O44. Codex 标题不同步进侧栏

现象（同上第一梯队 3）：用户已拍板用上游语义（数据库单一事实源），排
第三批。日志未立条。
状态：**已修**。`95300618` Merge wt/title-sync: list 读时写入 DB +
locked-title CAS（2026-08-20 00:43）。

### O45. 「待回复」独立面板 vs 会话中心过滤拥挤

现象（2026-08-19 22:54）：用户不懂「待回复面板」——有 agent 没回话为啥
系统不催（接到 O30）；为啥不直接做会话中心的一个过滤、单独面板是否
多余。另：会话中心过滤条件非常拥挤。后来（17:20）又要求文案改成
「有已读未回 / 发出去还没被回」之类更明白的说法（O31 已收语义，没收
入口形态）。
影响：O5 把七 facet 当验收通过；用户随后认为拥挤且入口重复。信息架构
和催办预期打架。
状态：文案/方向性 tooltip 已走 O31（`68094fe6` / `ada33623`）。**入口还
是不是独立面板**、过滤拥挤，仍排队。催办静音见 O30。

### O46. 任务看板：有引擎没仪表盘（用户提议引入 Trellis/multica 式看板）

现象（用户提议 2026-08-21 深夜）：参照 openagents Trellis Card 四态
（规划中/运行中/等待授权/已完成）与 multica 看板，希望 codeg 也能"一眼
看到任务处于什么进度"——动机是今晚的真实痛感：提了一堆问题却看不到状态、
还发生过漏记。
现状（代码实证）：codeg 的 work_task 生命周期**比 Trellis 更细**——
todo→queued→preparing→running⇄awaiting_input→review→merging→done +
failed/canceled，带 CAS 转移不变量（work_task.rs:4-40）；"待办任务"面板
已存在（智能体独立工作树干活、人 review 合并）。真正缺的三件：
① **看板可视化**（四泳道是我们 10 态的合理粗化分组：规划中={todo}、
运行中={queued/preparing/running/merging}、等待授权={awaiting_input/
review}、已完成={done}）；② **覆盖面**——Room 编队派工、多 agent 协作
不走 work_task，状态对人不可见（今晚编队三线就是活例）；③ **"等待授权"
的主动召唤**——blocked-on-human 却不喊人，正是用户"我不清楚状态"的核心
痛（与 O45 待回复面板、O34 状态可见同族）。
裁决（领导）：**立项，设计先行**。不引入外来任务系统（我们的状态机更强），
做"仪表盘补课"：已派 r-taskboard 只读解剖 multica/openagents/
openagents-inc/openteams 四家的任务模型与看板 UI，报告到后领导出设计 RFC
（泳道映射、卡片信息、与待办任务面板/会话中心的信息架构关系、Room 派工
是否落 work_task），RFC 过用户后排期施工。不挤今晚 DoD。
状态：立项·侦察中。
侦察回报（2026-08-21，r-taskboard）——**前提修正**：codeg 早已有四泳道
看板！`src/components/tasks/board-columns.ts` 把 10 态粗化成 todo/
inProgress/attention/done 四列，任务页有看板/列表双视图、拖拽、attention
琥珀色高亮。所以①"做个看板"是伪需求。真缺口重排为：
① **awaiting_input 无主动召唤**——任务卡等人时不发系统通知，用户不盯
页面就永远不知道（multica 的 inbox_item 按 severity 升级通知是最好的参照）；
② 分组维度写死（只按状态，不能按 agent/仓库分组）；③ attention 泳道语义
混杂（awaiting_input 和 failed 挤一列，轻重不分）；④ 卡片无 agent 活动
指示器（openagents 的 IssueAgentActivityIndicator 参照）。
另一裁决维持：**不要把 Room 编队派工硬塞进 work_task**——两者生命周期
语义不同，硬统一会造出第二个双时间线问题。RFC 以"增量补课"为纲：现有
看板上加召唤层/分组/活动指示，不另起炉灶。等用户过目。
RFC 落库（2026-08-21，领导执笔）：`TASKBOARD-RFC-2026-08-21.zh-CN.md`。
新增关键实证：**召唤层其实已存在**——tasks-view-context.tsx:162-183
`notifyFlips` 对 review/failed 翻转发系统通知，唯独 :170 的过滤条件把
awaiting_input 排除在外，这就是"等授权不喊人"的确切一行；通知管道
（事件推送→diff→系统通知，Provider 挂布局层）全链路已通，第一期只需
在现有函数里放行 + cooldown 防疲劳 + i18n×10。三期切分 + 待拍板 A–F
见 RFC。状态：**RFC 已备好等过目**。

### O47.（严重）Opus XHigh 工具循环狂烧 Claude 额度

现象（用户 2026-08-21 02:49 截图 API 网关）：密钥 `cc`、模型 `claude-opus-5`、
推理 **XHigh**、入站 `/v1/messages`、同一 IP。几秒一条：

- 02:47:27 缓存读 188.3K / 写出 1.6K / $0.14 / 总时 29.8s
- 02:47:57 缓存读 190.0K / 写出 **133** / $0.10 / 总时 4.2s
- 02:47:59 缓存读 190.5K / 写出 **114** / $0.11 / 总时 3.5s
- 02:48:04 同上量级
- 02:48:27 缓存读 191.3K / 写出 1.2K / $0.23

协调者当场判断：指纹指向 **codeg 里编队协调者会话的工具循环**（不是
O9 那种一次 455k 的幽灵会话）。随后查现场时发现 Tauri 后端已死、
把 6 条 queued 改成 paused。会话记录里**没有写下根因结案**。

影响：即便在「干活」也是 XHigh + 每步 19 万缓存读，约 $0.1–0.2 / 几秒，
空转时更是纯烧钱。无人值守编队会把订阅/中转额度打穿。O9 已修的是
「@ 空闲目标克隆新会话」；这条是**已有会话不停打 API**。

方向：未结案。要分清三件事：① 协调者自己工具空转（MCP 失败重试、
read 循环）；② PromptQueue/催办把同一 Session 反复 wake（O30 是短路
静音，这里可能是反面：一直有可 claim 的 reminder）；③ 进程死了队列
还在被别的路径重放。先看该会话 transcript 最后几十步是不是同一条
工具失败，再看 `conversation_prompt_queue_item` 当时 6 条 paused 的
source。要有回合/工具预算或失败熔断，不能只靠人手点停。
（本节已被下一节的定性结论取代，保留作过程记录；勿按本节"未结案"行事。）

### O47.（重大·已定性）API 额度剧耗告警：不是失控 bug，是编队经济性 + 燃烧不可见

现象（用户 02:49 实时截图告警）：账单面板 claude-opus-5/XHigh 每几秒一条
调用，每条 ~19 万缓存读、$0.10-0.23，用户判断"codeg 出 bug 疯狂消耗额度"。
定性（转录对账，02:40-02:49 UTC+8）：**正常行为、经济性差、可见性为零**。
- 该窗口是**两个** opus5 会话并行干活：协调者 37 步（Bash×16/Edit×3/发群聊
  ×1）+ 另一工位 31 步（Bash×9/Write×3/Edit/read_room…），合计 ~68 步
  ≈7 调用/分，与账单密度吻合；产出实在（wt/o33、wt/o34、wt/charter1 的
  提交都在）。
- 排除失控：队列无催办风暴（40 分钟仅 1 条 reminder）、无 O9 式幽灵会话、
  调用形状=典型 agent 工具循环（输入 2 token+全缓存续跑）。
- 成本机理：**190K 上下文 × 每步全量缓存读 × 碎步工具循环**——单步 $0.1+，
  两会话高峰 $40-90/小时。XHigh 是我（领导）配置的，属决策失误一半。
连带事故（另记）：02:52-53 dev 后端进程 exit 1 死亡（日志无 panic 记录）；
死因盘查后钉为**磁盘耗尽**（同时刻 D 盘一度只剩 601MB，见 O49）；重启时
调度器会自动唤醒 6 条排队编队消息——已手工置 paused 防复燃（备份
queued-items-backup-0303.txt）。
用户处置（已执行）：编队暂停施工，所有委托改走 grok build 桥（本地 grok
CLI，便宜）；sonnet/opus 子代理停用。
产品启示（并入 O46 看板 + O34 状态可见）：**燃烧率必须可见**——会话卡/
看板应显示每会话调用频次与近时开销估算；"等待授权"之外还要"正在烧钱"
这一维度。长上下文会话应主动提示/自动压缩（190K 不压缩=每步交学费）。
状态：已定性·防复燃已做·可见性需求并入 O46/O34 批。

### O48. 群消息上下文经济学：投递面有界，read_room 列表逐帖全文是唯一敞口

现象（用户 2026-08-21 凌晨提问）：多 agent 群聊时，读群消息是增量还是
一次读一大堆？会不会上下文爆炸？
代码实证（三层盘点）：
- **投递面全有界**：@ 唤醒的首投信封只带该帖正文前缀（≤8,000 字符，
  `MAX_FIRST_DELIVERY_BODY_CHARS`，超长带截断标记+补读指引）+ 父帖摘录
  ≤200 字符；催办摘要**永不带正文**、最多 8 行标题（collaboration_reminder.rs
  "催办不含正文"）；store_only 批量信封有逐回合总字节上限。
- **增量机制已存在**：`read_room unread=true` 从本会话 last-read 游标起
  读、读完推进游标——这就是增量读取；且 @ 信封自带正文，单点名场景 agent
  根本无需再调 read_room。
- **唯一敞口**：`read_room` 默认模式（不传 unread）返回最新窗口 50 帖
  （上限 200），`body: event.body` **逐帖全文无截断**（commands/
  collaboration.rs:1047；合法单帖上限 1MB）。若 agent 习惯性裸调默认模式
  而非 unread=true，N 个 agent 各自反复重读同一批全文——这是真实的
  平方级爆炸向量。
方向（设计项，动 MCP 工具行为，走契约流程）：① 列表模式逐帖正文加截断
（如 2,000 字符+bodyTruncated 标记）；② read_room 增加 event_id 参数做
单帖全文补读（否则截断会把"补读长帖"的既有指引打断——首投截断提示让
agent 去 read_room 读全文，不能两头都截）；③ 工具描述把 unread=true 扶正
为追赶场景的第一选择。
状态：已查明·设计项排队（含一处待拍板：截断阈值与默认行为）。

### O49. D 盘耗尽事件与磁盘卫生制度

现象：用户发现 1.9T 的 D 盘只剩 131G；关联 02:52 dev 后端 exit-1 崩溃
（当时一度只剩 601MB）。
盘查结论：元凶是 **worktree 的 cargo target 目录**——一次完整桌面 debug
构建 16-23G，worktree 用完即弃但 target 从不清理。当晚存量：fork-rewind
23G + 共享 _cargo-o33 23G + fork-at-message-be 16G + 旧 sessionmux 线 32G
+ 主仓 target 36G + target-gate 22G。
处置（2026-08-21 完成）：删 23 个已并入 worktree（树+分支）、17 个已并入
agent 隔离树、未并入树只删 target 保留工作区；**回收 91G+（131G→222G）**。
主仓 target（dev 在跑）与 target-gate（门禁在用）保留。
制度：立 `DISK-WORKTREE-HYGIENE.zh-CN.md`——worktree 合并即拆（Windows
要补 rm -rf 核实）、worktree 默认禁跑 cargo、磁盘地板 100G/警戒 150G、
无征兆崩溃先查磁盘。
遗留待用户定夺：`worktrees/sessionmux-session-timer`（codex/session-timer
未并入，树保留）与 `.claude/worktrees/agent-a31e34…`（领先 41 提交的
qoder/chat 旧功能线）是否还要。
状态：**已处置**·制度已立。

### O50. Windows 桌面 cargo test 载入即死（STATUS_ENTRYPOINT_NOT_FOUND）

现象：门禁四号批 TEST-DESKTOP 红——测试 exe 一个测试都没跑就死于
`0xc0000139`；整清 target-gate 全量重建依旧。server 面测试同代码全绿。
诊断（PE 导入表逐 DLL 对导出表核查）：唯一缺失导入是
`comctl32.dll!TaskDialogIndirect`（tauri-plugin-dialog→rfd 静态引入）。该
函数只在 Common-Controls **v6** SxS 程序集里；主应用 exe 有 tauri-build
嵌的 manifest 能解析到 v6，**cargo 测试 exe 只带 rustc 默认 manifest**，
Windows 便解析到 System32 的 5.82 存根 → 载入即死。这意味着任何 Windows
贡献者的 `cargo test --features test-utils` 都是坏的。
修复：build.rs 对 windows-msvc 目标发
`cargo:rustc-link-arg-tests=/MANIFESTDEPENDENCY:...Common-Controls 6.0.0.0`，
把 v6 依赖并进链接器嵌入的 manifest。实证：修后测试 exe `--list` 正常载入，
全量桌面测试恢复运行。
状态：**已修**（60ff8b3e）。

### O51. 新建群聊流程反直觉（用户 2026-08-21 白天）

现象（用户原话）："新建群聊我以为是新出来一个面板，然后在群聊里再拉人。
这个新建群聊也不太符合我们的预期，ui感觉很奇怪也不对""或者说按钮太小了？
还是说应该做其他交互，我也没想好，你看着办吧"。
现状（本人读码核实）：建群弹窗（create-room-dialog.tsx）max-w-sm 小 modal，
**必须勾满 2 个会话才允许建**（前端门槛 + 后端硬校验
collaboration_room_service.rs:272）；**第一个勾选的会话悄悄成为房主**
（:94，无提示）；房内拉人 UI 已存在但藏在"⋯"菜单项（rooms-page.tsx:1126）
和成员侧栏 h-6 w-6 幽灵"+"（:1199）里——用户"按钮太小"的直觉完全命中。
结构性约束：created_by_conversation_id 不可空（human 发帖借房主记账、
root folder 从房主推导），**零成员空群做不了**，最小可行=发起人 1 人。
方案（领导已定，用户"你看着办"授权）：① 后端最少成员 2→1；② 建群弹窗
改"标题+发起人单选（预选当前激活会话）"，建完即开面板（现有行为保持）；
③ 房内拉人显眼化：头部带文字按钮 + 空群时时间线换邀请引导块；④ bulk
action bar 的"多选会话→建群"路径不动。真正的"空群"等 human 一等公民化
schema 手术（模型体检 RFC ②）落地后再说。
状态：施工中（grok run-mt2csc5j，wt/room-create）。

### O52. 会话中心排序换"最近活动"（用户委托拍板）

用户："没看懂，你决定吧，会话中心本身不就是最近活动排序的吗"——用户
心智模型就是最近活动。裁决：排序+时间列**一起**换 updated_at/updatedAt
（保住"排序键=打印值"的一条递减钟不变量，这正是昨夜自撤时立的规矩）。
四处同改（两个 sortKey、两处时间列打印）+ 搜索合并中间排序对齐 + 设计
注释重写。313 前端测试全绿。契约待拍板 5 已结。
状态：**已修**（领导亲改，本批提交）。

### 2026-08-21 白天：用户拍板批（八条一次性到位）

1. O8 做（A0+B-lite；A1/B-full 维持不做）→ 已派工。
2. O46 做 + 追加组织方式调研（分项目？分群聊？Collection path？参照
   vibe-kanban/multica/openagents）→ 第一期已派工，调研已派工。
3. 排序委托领导 → O52 已落地。
4. O48 不做硬截断，改逐步披露接口（像 Read 工具）→ 已派工。
5. GitHub 纠正（重要）：`origin = github.com/xintaofei/codeg` 是**上游
   开源主仓不是我们的远端**——"永不 push"铁律实属万幸。要有自己的远端
   只能 fork（公开）或自建私库（可私有），等用户选形态。
6. schema 手术类用户没看懂 → 领导在回复里用人话集中解释（见会话记录）。
7. O45 结案（不做独立面板）；O39 做，先调研 paseo/monet → 已派工。
8. O47 定性要复查（正常还是异常，不吃老结论）→ 本批收尾后重核。

### O53. 群聊每条消息带时间（用户 2026-08-21 白天）

现象（用户原话）："群聊的消息面板，我说的时间线，是说每个消息都带时间，
就和session面板一样，这样不就自然有时间线了吗"。
根因（读码核实）：非聚合帖头部有 `<time>`（rooms-page.tsx :273-275 原行号），
但 sameSpeaker+5 分钟聚合的后续消息整个头部不渲染——**聚合行完全没有
时间戳**，槽位只有一个悬停才显形的回复按钮（静止时视觉为空）。
修法：聚合行左轨（w-9）常显紧凑 24h 时间（`formatRoomTimeCompact`，
12h 地区"02:23 PM"塞不下 36px 所以强制 24h；带 dateTime 属性），悬停/
聚焦时与回复按钮透明度互换（时间淡出、按钮淡入），互不挤占。聚合逻辑
本身未动。
状态：**已修**（领导亲改；62 rooms 测试全绿 + eslint 干净；待 CDP 走查
像素级确认）。

### O47 复查（2026-08-21 白天，用户点名："是真协同还是设计让 agent 停不下来"）

证据链（三层，全部本人实证，不吃昨晚老结论）：
1. **账单↔转录逐条对上**（用 opencli 桥读 sub2api 用量页）：账单尾部
   02:51:35→02:53:53 的 opus/XHigh 记录（每条 ~220K 缓存读、$0.12-0.34）
   与协调者转录（conv314，2e26fd1c）最后几个回合一一对应；02:53:53 后
   opus 零记录——**暂停止血有效，此后未复燃**。范围内 opus-5 共 474 次
   $111.47；另 fable-5（领导主会话）77 次 $60，如实入账。
2. **回合结构分解**（两份转录按唤醒源拆回合）：
   - 工人（60b1a19b）：干净——1 条任务书 → 一口气 80 步做完包 I 补工，
     +1 次群提醒 27 步。纯真活。
   - 协调者（2e26fd1c，936 步生涯/47 回合）：事发窗 9 个回合，唤醒源
     交替为「群消息投递」和「Codeg 系统提醒：N 条未读群消息」，每
     3-10 分钟被拽起一次，每次 16-49 步 × 220-300K 缓存读；18:19Z 还
     发生一次上下文耗尽压缩（重建缓存另交学费）。
   - 产出实在：wt/fork-rewind 分支 package G/H 的 forkAtMessage 前后端
     都已合、门禁绿——**不是空转永动机**。
3. **今晨新发现并已处置**：暂停操作之后 19:21/19:24Z 又进来 2 条
   mailbox-attention 催办，一直处于 queued（指向已 cancelled 的编队
   会话），重启/重派会复燃——已按昨晚同款手工置 paused（现 11 paused
   / 0 queued）。另记：协调者转录文件 mtime 今晨 09:29 被空触碰但无
   新内容（无新调用，无害，不深究）。

**裁决（回答用户的问题）**：grok 说的"正常协同"对一半。①确实在干真活
（有合并产出为证）；②但设计上存在三个费用放大器：（a）未读摘要提醒对
expects_reply=false 的帖子也会唤醒协调者——"停下来"的条件是群里彻底
安静+无未回义务，活跃编队里近似永不停；（b）每次唤醒支付全上下文缓存
读（190-300K），碎步工具循环把它乘几十倍；（c）opus+XHigh 档位是领导
配置失误（已认）。③队列在进程死亡后仍保留 queued 项并会在重启时自动
重放（本次抓到 2 条漏网）——这是真设计洞。
**建议的三个设计修复（等拍板）**：A. 唤醒分级——仅 @提及/expects_reply
立即唤醒，FYI 帖并入下次自然唤醒（顺 O30 短路方向）；B. 燃烧可见性
（已在 O46 RFC 缺口⑤）；C. 后端重启后，滞留超过阈值的 queued 协作/催办
项须人工恢复而不是自动重放。
状态：**复查完成·定性修订**（真活+设计放大器并存）；三个修复项等用户
拍板。

### O47 复查·二挖（用户追问"看过交互详情吗"，2026-08-21）

把协调者三个被"未读摘要"叫醒的回合逐步拆到工具调用级（本人实证）：
- 18:22 醒（49 步）：读群 → 判断包回执 → **合并 package J 并亲跑门禁**
  → 查 323/324 会话状态 → 发群通报。其中 ~6-8 步是 MCP 工具词汇表
  现学现用（开销），主体是真协调。
- 18:40 醒（17 步）：机械核验包 I 的声明 → 发现规格行锚过期 → 3 个
  Edit 修自己的规格文档 → 提交 → 发群。全是真活。
- 18:45 醒（3 步）：在 charter1 跑 cargo 门禁（账单里那条 1m39s/$0.34
  的长调用就是它）。真活。
**修订结论：不存在互相唤醒的无意义空转**。每次醒来都推进了合并流水线；
贵在"每次醒来重付 190-300K 全上下文缓存读 × opus/XHigh 档位"，是价格
问题不是行为问题。
**修复建议随之修订**：A（唤醒分级）**降级为不急**——本样本里每次唤醒
都有事干，分级反而可能延误协调；B（烧钱可见）保留（已在 O46 RFC）；
C（重启不自动重放滞留队列项）**维持建议**——与空转无关，是"进程死后
队列突袭复活"的真设计洞（今晨 2 条漏网即实证）。

### O56. 房间成员管理：一件事散在四个组件里（用户 2026-08-21 白天实机复审）

用户三连问（原话）："这个加入session的大按钮不是重复的吗，有超多入口，这个就没
必要了吧"／"加入session的时候尽管可以搜索，但是面临重复以及重名的问题，要不要把
这个用会话中心来处理"／"这套管理机制要不要整理一下或者重构设计，这种跨越多个组件
的设计，怎么维护和设计起来方便不容易出bug？"
现状实证（本人读码 + 用户控制台报错）：
- **四个入口**：房间头部大按钮（O51 刚加）、⋯ 菜单项、成员侧栏 6×6 的「+」、
  空群引导；外加侧栏批量建群是第五处。每处**各自**写一遍"搜候选→勾选→调 API→
  toast→刷新"。
- **真 bug**：添加成员弹窗的行是 `<button>` 里套 Radix `<Checkbox>`（它自己渲染
  `<button>`）→ 浏览器 hydration error `<button> cannot be a descendant of
  <button>`。这个错误在 O51 工人报告里作为 jsdom 警告被如实记录过，但当时任务书
  写了"不动 add-member 弹窗"，所以没修——**教训：报告里出现的"既有警告"要当线索
  追，不要因为不在本次范围就放过**（已进 LESSONS 候选）。
- 重名实证：用户侧栏截图里 5 个会话都叫「FDF综述」，选人列表只印标题=不可分辨。
裁决（领导，用户"你看着办"授权）：
1. **抽一个 `<SessionPicker>` + 一个 membership 服务**，所有入口退化成薄触发。
   这就是对"跨组件怎么不出 bug"的回答：**同一件事只有一个实现，入口只负责触发**；
   button 嵌套这类问题只可能在一处犯，修一次全好。
2. **删掉头部大按钮**（保留 ⋯ 菜单 + 放大成员侧栏「+」+ 空群引导）。
3. **消歧靠行内元信息**（文件夹名 + 相对时间），**不**把选人交给会话中心——
   模态套模态过重，而重名问题的本质是"行信息太少"，不是"选择器不够强"。
4. 回归钉：picker 测试断言 `container.querySelectorAll("button button").length === 0`。
状态：施工中（run-mt2faacf，wt/room-picker）。

### O57. 成员关系只能从群这一侧建立

用户："要不要给session面板加入一个加入群聊room的选项或者按钮，这样我们也能从某个
session的pane触发，把这个session加入到某个群聊里？"
现状：只有房间页能加人，会话侧无反向入口。裁决：**做**，但按用户刚给的"不要再加
大按钮"原则，落在会话面板 ⋯ 菜单里，弹一个轻量房间选择器（列表带成员数与归属，
同样消歧优先）。状态：施工中（run-mt2faayo，wt/join-room）。

### O58. 侧栏多选建群——查证结果：本就已经有了

用户问"要不要在sidebar加入多选功能，可以多选几个session创建群聊，群聊创建的位置
与第一个选择的session同collection"。
读码实证：**功能已存在且行为正好就是用户描述的那样**——`session-bulk-action-bar.tsx`
挂在分类树上（collection-tree.tsx:2642），建群时
`createdByConversationId = conversations[0].id`（第一个选中的当发起人），
`collectionId` 取全部选中会话的共同 collection（`listConversationCollectionRefs`
去重后仅当只有一个才带上，:454-458）。
裁决：**不新建功能**，真缺口是**可发现性**（用户用了这么久都不知道有）。排队项：
多选入口的自明性（如何进入多选态需要提示）。状态：已查证·可发现性排队。
